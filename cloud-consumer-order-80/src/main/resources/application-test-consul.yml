server:
  port: 80

spring:
  application:
    name: cloud-order-service
  cloud:
    consul:
      # 这三个值都是默认值
      host: localhost
      port: 8500
      discovery:
        service-name: ${spring.application.name}
